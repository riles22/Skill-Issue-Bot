# [voice] pulls in PyNaCl and davey, both required for Discord voice
# (davey implements the DAVE end-to-end encryption protocol)
discord.py[voice]>=2.4,<3.0
# No upper bound on purpose: yt-dlp must stay current or YouTube extraction breaks
yt-dlp>=2025.6.9
python-dotenv>=1.0,<2.0
